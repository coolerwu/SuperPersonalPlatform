auth:
  token: "change-me"
proxy:
  upstream_base_url: "http://192.168.1.3:9119/"
server:
  host: "0.0.0.0"
  port: 8888
llm:
  default_model_id: "default"
  models:
    - id: "default"
      name: "默认模型"
      provider: "openai_compatible"
      base_url: "https://api.openai.com/v1"
      api_key: "change-me"
      model: "gpt-4o-mini"
      mode: "prompt"
      temperature: 0.7
      supports_images: true
skills:
  definitions: []
portfolio:
  agent_id: ""
channels:
  wechat_personal:
    enabled: false
    default_agent_id: ""
    # Multi-account format (preferred):
    accounts:
      - id: "main"
        name: "主账号"
        default_agent_id: ""
        auto_start: false
        # Optional explicit proxy for iLink HTTP client.
        proxy: ""
    # Legacy single-account format (used when 'accounts' is absent):
    # enabled: false
    # default_agent_id: "assistant"
    # auto_start: false
    # proxy: ""
agents:
  definitions: []
